# --- Required ---
# Also set in Coolify (app > Environment Variables)
DATABASE_URL=postgresql://user:password@localhost:5432/gdpr_audit

# --- Optional: AI analysis ---
# Also set in Coolify
OPENROUTER_API_KEY=

# --- Optional: Auto-fix (Webflow sites) ---
# Also set in Coolify when ready to use fix automation
WEBFLOW_API_TOKEN=

# --- Optional: Auto-fix (GTM tags/triggers) ---
# Also set in Coolify when ready to use fix automation
GTM_API_TOKEN=
